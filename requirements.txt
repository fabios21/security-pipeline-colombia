# Dependencias mínimas para Security Pipeline Colombia
# Estas dependencias se instalan automáticamente cuando se ejecuta la acción

# Core dependencies para análisis de seguridad
jq>=1.6.0
jsonschema>=4.0.0
jinja2>=3.0.0
python-dateutil>=2.8.0
pyyaml>=6.0